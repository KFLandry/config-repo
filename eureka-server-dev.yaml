eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: ${EUREKA-URI:http://${spring.application.name}:${PORT:8761}/eureka}

  instance:
    hostname: ${URL-API-GATEWAY:${spring.application.name}}
    prefer-ip-address: true
    non-secure-port: ${PORT:8761}

logging:
  level:
    root: warn
    com.netflix: OFF
    com.discovery: OFF
